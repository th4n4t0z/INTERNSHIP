let arr = [2,3,5,6,8,11]

for (let index = 0; index < arr.length; index++) {
    if(arr [index]%2==0){
   console.log(`${arr [index]} is even`);
    }
   else{
       
    console.log(`${arr [index]} is odd`);
    }
}